# Copy to .env and adjust if needed
PORT=3000
HOST=0.0.0.0
# When unreachable, the app falls back to the JSON file adapter (data/*.json).
MONGO_URI=mongodb://127.0.0.1:27017/ca_inter
MONGO_TIMEOUT_MS=2500